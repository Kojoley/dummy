sudo: false
os: linux

language: cpp

apt:
  sources:
    - llvm-toolchain-trusty-5.0
    - ubuntu-toolchain-r-test
  packages:
    - clang-5.0
    - libstdc++-7-dev

env:
  matrix:
    - JOB=1
    - JOB=2
    - JOB=3
    - JOB=4
    - JOB=5
    - JOB=6
    - JOB=7
    - JOB=8
    - JOB=9
    - JOB=10

script:
  - clang-5.0 --version
